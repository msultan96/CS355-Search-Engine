<!DOCTYPE html>
<html>
<head>
    <title><%= title %></title>
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css">
    <!-- Bootstrap core CSS -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/4.1.3/css/bootstrap.min.css" rel="stylesheet">
    <!-- Material Design Bootstrap -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/mdbootstrap/4.5.13/css/mdb.min.css" rel="stylesheet">
    <!-- Custom Styles -->
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <link rel="stylesheet" href="/stylesheets/custom.css" />
</head>
<body class="bg-gradient-light d-flex">
<div class = "container align-self-center">
    <div class="jumbotron bg-dark text-white shadow-lg d-block align-self-center disable-select text-center">
        <div class="text-center">
            <h1 class = "display-4">O O P S</h1>
            <% console.log(error.status) %>
            <%if (error.status === undefined) {%>
                <h4 class = "text-center"
                    style = "font-size: 1.5rem; font-weight: 300; ">
                    Something seems to have gone wrong!</h4>
            <%} else { %>
                <h4 class = "text-center d-none d-sm-block"
                    style = "font-size: 1.5rem; font-weight: 300; ">
                    We've come across a <mark><%= error.status%></mark> error!</h4>
                <h4 class = "text-center d-sm-none"
                    style = "font-size: 1.5rem; font-weight: 300; ">
                    We've come across a <%= error.status %> error!</h4>
            <%} %>
        </div>
        <div class ="lead">
            <% if (error.status == "400") { %>
                <p>This is a <strong>Bad Request</strong> error! You might have gotten this error because of the following: </p>
                <ol>
                    <li>You may need to clear your browsers cache</li>
                    <li>You may have entered an incorrect HTTP requests</li>
                    <li>You might need to update your browser</li>
                </ol>
            <%} %>

            <%if (error.status == "401") { %>
                <p>This means that you are <strong>unauthorized</strong> to access this page.</p>
            <%} %>

            <%if (error.status == "403") { %>
                <p>This means that you don't have <strong>permission</strong> to access this page.</p>
            <%} %>

            <%if (error.status == "404") { %>
                <p>This means that the page you were trying to access <strong>doesn't exist</strong>!</p>
            <%} %>

            <%if (error.status == "500") { %>
                <p>This mean's that we're experiencing an issue on our end.</p>
            <%} %>

            <%if (error.status == "504") { %>
                <p>This is a <strong>Gateway Timeout</strong> error! You might have gotten this error because of the following: </p>
                <ol>
                    <li>You may need to check your connection(especially if you're on Queens College Wifi!)</li>
                    <li>Our side is experiencing heavy traffic!</li>
                </ol>
            <%} %>
        </div>
        <div class ="d-flex justify-content-end lead" style="font-size:16px;">
            <a onclick="history.go(-1)">
                <i class="fas fa-arrow-alt-circle-left" style="font-size:30px;"></i> Return to the previous page . . .
            </a>
        </div>
    </div>
</div>
</body>
<div id = "included-javascript">
    <!-- JQuery -->
    <script type="text/javascript" src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>
    <!-- Bootstrap tooltips -->
    <script type="text/javascript" src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.4/umd/popper.min.js"></script>
    <!-- Bootstrap core JavaScript -->
    <script type="text/javascript" src="https://cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/4.1.3/js/bootstrap.min.js"></script>
    <!-- MDB core JavaScript -->
    <script type="text/javascript" src="https://cdnjs.cloudflare.com/ajax/libs/mdbootstrap/4.5.13/js/mdb.min.js"></script>
    <link rel='stylesheet' href='https://use.fontawesome.com/releases/v5.5.0/css/all.css' integrity='sha384-B4dIYHKNBt8Bc12p+WXckhzcICo0wtJAoU8YZTY5qE0Id1GSseTk6S+L3BlXeVIU' crossorigin='anonymous'>
</div>
</html>


